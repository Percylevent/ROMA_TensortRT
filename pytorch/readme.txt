1.dino.py，roma.py，gim_roma_100h.ckpt，matcher_roma.py： A simplified gim_roma pytorch code, show the pytorch network.

2.extra.py: to extra the core part from roma to onnx model,you can get the 

3.onnxsimple.py: simplify the onnx model

4.testonnx.py: test the extraed onnx model

